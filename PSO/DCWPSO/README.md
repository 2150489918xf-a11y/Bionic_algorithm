# DCWPSO - 动态聚类权重粒子群优化算法

## 📖 简介

这是DCWPSO（Dynamic Clustering Weight Particle Swarm Optimization）算法的Python实现，从MATLAB代码转换而来。

## 🔑 核心特性

1. **动态邻域聚类**：根据欧氏距离动态确定粒子邻居
2. **余弦相似度判断**：智能选择学习策略
3. **最差个体学习**：后期阶段利用最差个体信息跳出局部最优

## 📦 依赖库

```bash
pip install numpy matplotlib
```

## 🚀 快速开始

### 基本使用

```python
from dcwpso import DCWPSO
import numpy as np

# 定义目标函数
def sphere(x):
    return np.sum(x**2)

# 创建优化器
optimizer = DCWPSO(
    fitness_func=sphere,
    dimension=30,
    particle_number=30,
    max_fes=10000,
    lb=-100,
    ub=100,
    k_neighbors=2
)

# 执行优化
gbest, gbestval, curve = optimizer.optimize()

print(f"最优解: {gbest}")
print(f"最优值: {gbestval}")
```

### 运行测试示例

```bash
python test_dcwpso.py
```

这将测试5个经典基准函数并生成收敛曲线图。

## 📊 算法参数说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `fitness_func` | 目标函数 | 必填 |
| `dimension` | 问题维度 | 必填 |
| `particle_number` | 粒子数量 | 必填 |
| `max_fes` | 最大函数评估次数 | 必填 |
| `lb` | 搜索空间下界 | 必填 |
| `ub` | 搜索空间上界 | 必填 |
| `k_neighbors` | 邻居数量 | 2 |

## 🔬 算法原理

### 1. 动态惯性权重
```python
iwt = (rand * (MaxFes-FES)^2 / MaxFes^2) * (0.9-0.4) + 0.4
```

### 2. 余弦相似度策略
- **相似度 < 0.5**：使用邻域学习
- **相似度 ≥ 0.5**：使用标准PSO

### 3. 最差个体学习（后期80%）
利用最差个体信息帮助跳出局部最优

## 📈 测试结果

算法在以下基准函数上进行了测试：
- Sphere（单峰）
- Rastrigin（多峰）
- Rosenbrock（山谷型）
- Ackley（多峰）
- Griewank（多峰）

## 📝 文件说明

- `dcwpso.py` - DCWPSO算法核心实现
- `test_dcwpso.py` - 测试脚本和基准函数
- `README.md` - 使用文档

## 🔄 与MATLAB版本的差异

1. 使用NumPy数组代替MATLAB矩阵
2. 使用Python的面向对象设计
3. 优化了邻居搜索的效率
4. 添加了更完善的边界处理

## 📧 联系方式

如有问题或建议，欢迎反馈！

---
**转换日期**: 2026-01-30
**原始版本**: MATLAB
**Python版本**: 3.7+
